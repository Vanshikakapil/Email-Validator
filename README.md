# 📧 Email Validator Application

A full-stack Email Validation Web Application built using **FastAPI (Python)** for backend and **React + Vite + Tailwind CSS** for frontend.

This application validates single and bulk email addresses using DNS and SMTP verification techniques.
